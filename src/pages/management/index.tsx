import { Box, Text } from "@chakra-ui/react";
import { Layout } from "@widgets/layout";

const Page = () => {
  return (
    <Layout>
      <Text as="h2">Руководство</Text>
    </Layout>
  );
};

export default Page;
